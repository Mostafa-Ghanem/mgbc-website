import { getCollection } from 'astro:content';
export async function GET(){const pages=await getCollection('pages');const urls=pages.filter(p=>p.data.status==='PUBLISH_READY').map(p=>p.data.canonical);const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(url=>`<url><loc>${url}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
