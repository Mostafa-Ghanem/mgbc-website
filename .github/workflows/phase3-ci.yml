name: Phase 3 Astro CI

on:
  push:
    branches:
      - phase3/astro-assembly
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install
      - name: Astro check
        run: npm run check
      - name: Production build
        run: npx astro build
